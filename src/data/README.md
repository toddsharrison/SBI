# Data Directory

Contains JSON files that can be generated by the optional SQL export script (`scripts/export_charts.py`). The current React app does not read these files; they are reserved for future data-driven charts.
